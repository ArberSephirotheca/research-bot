# reports/

Daily Markdown digests generated by the bot. Files are named `YYYY-MM-DD.md`.

Each item includes authors and (when available from the feed) affiliations.

These reports can be ingested into the Discord RAG bot by passing `--reports-dir reports`
to `scripts/rag_ingest.py`.
